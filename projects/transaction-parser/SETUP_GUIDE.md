# 🚀 Покроковий гайд по налаштуванню

Детальна інструкція для початківців.

## Крок 1: Встановлення Node.js

### macOS
```bash
brew install node
```

### Windows
Завантажте з https://nodejs.org/ (LTS версію)

### Linux (Ubuntu/Debian)
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

Перевірте встановлення:
```bash
node --version  # Має бути >= 18.x
npm --version
```

## Крок 2: Налаштування Google Sheets

### 2.1 Створення Google Cloud проєкту

1. Перейдіть на https://console.cloud.google.com
2. Натисніть "Select a project" → "New Project"
3. Назва проєкту: `Transaction Sync`
4. Натисніть "Create"

### 2.2 Увімкнення Google Sheets API

1. У меню зліва: "APIs & Services" → "Library"
2. Знайдіть "Google Sheets API"
3. Натисніть "Enable"

### 2.3 Створення Service Account

1. У меню: "APIs & Services" → "Credentials"
2. Натисніть "Create Credentials" → "Service Account"
3. Заповніть:
   - Service account name: `transaction-sync`
   - Service account ID: автоматично згенерується
   - Description: `Service account for transaction sync`
4. Натисніть "Create and Continue"
5. Role: Select "Editor" (можна також "Basic" → "Editor")
6. Натисніть "Continue" → "Done"

### 2.4 Створення JSON ключа

1. Знайдіть створений Service Account в списку
2. Натисніть на email Service Account
3. Перейдіть на вкладку "Keys"
4. "Add Key" → "Create new key"
5. Виберіть "JSON"
6. Натисніть "Create"
7. Файл автоматично завантажиться
8. **Збережіть цей файл** - він вам знадобиться

### 2.5 Створення Google Sheet

1. Перейдіть на https://sheets.google.com
2. Створіть новий Sheet: "Blank"
3. Назва: "Transaction Tracker" (або будь-яка інша)
4. Скопіюйте ID з URL:
   ```
   https://docs.google.com/spreadsheets/d/1a2B3c4D5e6F7g8H9i0J/edit
                                          ^^^^^^^^^^^^^^^^^ це ваш SHEET_ID
   ```

### 2.6 Надання доступу Service Account

1. У вашому Google Sheet натисніть "Share" (кнопка вгорі справа)
2. Вставте email з Service Account JSON файлу (поле "client_email")
   - Приклад: `transaction-sync@project-name.iam.gserviceaccount.com`
3. Виберіть роль: "Editor"
4. Зніміть галочку "Notify people" (не потрібно відправляти email роботу)
5. Натисніть "Share"

## Крок 3: Отримання API ключів

### 3.1 Brex API Key

1. Увійдіть на https://dashboard.brex.com
2. Settings → Developer
3. API → Create API Token
4. Назва: `Transaction Sync`
5. Скопіюйте ключ (він показується тільки один раз!)

**Документація:** https://developer.brex.com/docs/

### 3.2 Stripe Secret Key

1. Увійдіть на https://dashboard.stripe.com
2. Developers → API keys
3. Для тестування:
   - Переключіть на "Test mode" (toggle вгорі справа)
   - Скопіюйте "Secret key" (починається з `sk_test_...`)
4. Для продакшену:
   - Переключіть на "Live mode"
   - Скопіюйте "Secret key" (починається з `sk_live_...`)

**ВАЖЛИВО:** Спочатку тестуйте з test key!

**Документація:** https://stripe.com/docs/api

### 3.3 PrivatBank API

#### Варіант A: API для фізичних осіб

1. Увійдіть в мобільний додаток ПриватБанк24
2. Меню → Налаштування → API
3. Створіть токен доступу
4. Скопіюйте токен

**АБО**

#### Варіант B: Merchant API

Якщо у вас є merchant ID:
- Використовуйте merchant credentials
- Потрібні: `MERCHANT_ID`, `MERCHANT_PASSWORD`, `CARD_NUMBER`

**Документація:** https://api.privatbank.ua

**ПРИМІТКА:** API ПриватБанку може вимагати попереднього налаштування або договору для доступу до виписок.

## Крок 4: Налаштування проєкту

### 4.1 Клонування та встановлення

```bash
# Перейдіть в папку проєкту
cd /path/to/tools/transaction-parser

# Встановіть залежності
npm install
```

### 4.2 Створення .env файлу

```bash
# Скопіюйте приклад
cp .env.example .env

# Відредагуйте файл
nano .env  # або vim, або будь-який редактор
```

### 4.3 Заповнення .env

```env
# Google Sheets - вставте SHEET_ID з кроку 2.5
GOOGLE_SHEET_ID=1a2B3c4D5e6F7g8H9i0J

# Шлях до JSON ключа (залиште як є, якщо файл в credentials/)
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./credentials/service-account.json

# Brex - вставте API key з кроку 3.1
BREX_API_KEY=bxp_prod_xxxxxxxxxxxxx

# Stripe - вставте secret key з кроку 3.2
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx

# PrivatBank - вставте токен або merchant credentials з кроку 3.3
PRIVATBANK_API_TOKEN=your_token_here
PRIVATBANK_CARD_NUMBER=1234567890123456

# Налаштування
INITIAL_FETCH_DAYS=30
DEBUG=false
```

### 4.4 Копіювання Service Account JSON

```bash
# Створіть папку
mkdir credentials

# Скопіюйте завантажений JSON файл
cp ~/Downloads/project-name-xxxxx.json credentials/service-account.json
```

## Крок 5: Тестування

### 5.1 Перевірка конфігурації

```bash
npm start test
```

Очікуваний результат:
```
🔌 Testing API connections...

📊 Connection Test Results:
────────────────────────────────────────
✅ Brex: OK
✅ Stripe: OK
✅ PrivatBank: OK
────────────────────────────────────────
```

### 5.2 Якщо є помилки

#### ❌ Google Sheets: "Failed to initialize"
- Перевірте що файл `credentials/service-account.json` існує
- Перевірте що Service Account має доступ до Sheet

#### ❌ Brex: "401 Unauthorized"
- Перевірте API ключ в .env
- Перевірте що ключ не має зайвих пробілів

#### ❌ Stripe: "Invalid API key"
- Перевірте що ключ починається з `sk_test_` або `sk_live_`
- Перевірте що ключ актуальний

#### ❌ PrivatBank: "Authentication failed"
- Перевірте токен або merchant credentials
- Для деяких операцій може знадобитись додаткове налаштування в банку

## Крок 6: Перший запуск

### 6.1 Синхронізація

```bash
npm start
```

### 6.2 Що відбувається

1. Програма підключається до кожного API
2. Завантажує транзакції за останні 30 днів (INITIAL_FETCH_DAYS)
3. Перевіряє які транзакції вже є в Google Sheet
4. Додає тільки нові транзакції

### 6.3 Перевірка результату

1. Відкрийте ваш Google Sheet
2. Ви повинні побачити 3 нові вкладки (tabs):
   - **Brex** - транзакції з Brex
   - **Stripe** - транзакції з Stripe
   - **PrivatBank** - транзакції з ПриватБанку
3. Кожна вкладка має заголовки та дані транзакцій

## Крок 7: Налаштування автоматизації (опційно)

### Варіант A: Cron (Linux/Mac)

```bash
# Відредагувати crontab
crontab -e

# Додати рядок для щоденного запуску о 9:00
0 9 * * * cd /path/to/transaction-parser && npm start >> logs/sync.log 2>&1
```

### Варіант B: GitHub Actions

1. Створіть GitHub репозиторій
2. Push код
3. Settings → Secrets and variables → Actions
4. Додайте всі secrets з .env файлу:
   - `GOOGLE_SHEET_ID`
   - `GOOGLE_SERVICE_ACCOUNT` (вміст JSON файлу)
   - `BREX_API_KEY`
   - `STRIPE_SECRET_KEY`
   - тощо
5. Workflow автоматично запуститься щодня

## Часті питання (FAQ)

### Q: Чи можна використовувати без одного з API?

A: Так! Закоментуйте або видаліть відповідні ключі в .env. Програма пропустить цей источник.

### Q: Як часто можна запускати синхронізацію?

A: Як завгодно часто. ID-based архітектура гарантує що дублікатів не буде.

### Q: Чи безпечно зберігати API ключі?

A: Ключі мають зберігатись тільки в:
- `.env` (локально, gitignored)
- GitHub Secrets (для автоматизації)
- Ніколи не комітьте їх в git!

### Q: Що робити якщо транзакція пропущена?

A: Просто запустіть синхронізацію ще раз. Всі транзакції будуть перевірені знову.

### Q: Чи можна змінити формат дат/сум?

A: Так, відредагуйте методи `transactionToRow()` в `src/sheet/google-sheet.js`

### Q: Скільки транзакцій можна зберігати?

A: Google Sheets підтримує до 10 млн клітинок. Це ~100,000+ транзакцій.

## Додаткові ресурси

- [Google Sheets API Documentation](https://developers.google.com/sheets/api)
- [Brex API Documentation](https://developer.brex.com/docs/)
- [Stripe API Documentation](https://stripe.com/docs/api)
- [PrivatBank API Documentation](https://api.privatbank.ua)

## Потрібна допомога?

1. Перевірте README.md
2. Запустіть `npm start test`
3. Увімкніть DEBUG режим: `DEBUG=true npm start`
4. Створіть Issue з логами помилок

---

**Готово!** Ваша система синхронізації транзакцій налаштована. 🎉
